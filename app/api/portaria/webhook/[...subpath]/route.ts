/**
 * Catch-all para sub-rotas do Monitor iDFace Control iD.
 * O firmware do iDFace anexa automaticamente sufixos como /dao, /catra, /door ao endpoint de monitor configurado.
 */
export { GET, POST } from '../route'
